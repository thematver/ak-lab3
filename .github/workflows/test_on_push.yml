name: My Workflow

on:
  push:  # will run on any push

jobs:
  prepare:
    uses: thematver/ca-actions/.github/workflows/full-workflow.yml@main
    with:
      python_version: 3.9
      pytest_workdir: test
      coverage_path: ../machine
      mypy: false
      flake8: false
      black: false
      isort: false
      
